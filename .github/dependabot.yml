version: 2
updates:
  - package-ecosystem: "bundler"
    directory: "/"
    schedule:
      interval: "weekly"
    groups:
      # Collapse the frequent RuboCop bumps into a single PR.
      rubocop:
        patterns:
          - "rubocop"
          - "rubocop-*"
